apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-apikey-page
  namespace: myhr-uat
  labels:
    app.kubernetes.io/name: demo-apikey
data:
  index.html: |
    <!doctype html><meta charset="utf-8"><title>demo-apikey</title>
    <h1>demo-apikey · HTTP 200</h1>
    <p>ถ้าเห็นหน้านี้ แปลว่า Gateway ตรวจ API key ผ่านแล้วถึงจะส่ง request มาถึงที่นี่</p>
    <p>ตัว app ไม่มี code ตรวจ API key อยู่เลยสักบรรทัด</p>
