apiVersion: v1
kind: Service
metadata:
  name: demo-apikey
  namespace: myhr-uat
  labels:
    app.kubernetes.io/name: demo-apikey
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo-apikey
  ports:
    - name: http
      port: 80
      targetPort: http     # = containerPort 8080
