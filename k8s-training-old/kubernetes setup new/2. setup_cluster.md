## on master 1
### move file in config/master1/haproxy to /etc/haproxy
mkdir -p /etc/haproxy
mv -f /opt/haproxy/* /etc/haproxy

### move file in config/master1/keepalived to /etc/keepalived
mkdir -p /etc/keepalived
mv -f /opt/keepalived/* /etc/keepalived
chmod +x /etc/keepalived/check_apiserver.sh

### move file in config/master1/kubernetes-manifests to /etc/kubernetes/manifests
mv -f /opt/kubernetes-manifests/* /etc/kubernetes/manifests



## init cluster

kubeadm init --apiserver-advertise-address=192.168.40.32 --control-plane-endpoint=keepalived-vir:80 --pod-network-cidr=10.242.0.0/16 --upload-certs

sudo cp -i -f /etc/kubernetes/admin.conf $HOME/.kube/config



## add pod network to the cluster

### Needs manual creation of namespace to avoid helm error
kubectl create ns kube-flannel
kubectl label --overwrite ns kube-flannel pod-security.kubernetes.io/enforce=privileged

helm repo add flannel https://flannel-io.github.io/flannel/
helm install flannel --set podCidr="10.242.0.0/16" --namespace kube-flannel flannel/flannel

# # check pod
kubectl get pod -A


## create secret for private repository 
kubectl create secret docker-registry regcred --docker-server=registry.myhr.co.th --docker-username=admin --docker-password=<REGISTRY_PASSWORD>

# # join master ได้มาจากการ init
# # รัน master หลัก
 mkdir -p $HOME/.kube
  sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
  sudo chown $(id -u):$(id -g) $HOME/.kube/config

Alternatively, if you are the root user, you can run:

  export KUBECONFIG=/etc/kubernetes/admin.conf

You should now deploy a pod network to the cluster.
Run "kubectl apply -f [podnetwork].yaml" with one of the options listed at:
  https://kubernetes.io/docs/concepts/cluster-administration/addons/

You can now join any number of the control-plane node running the following command on each as root:
# # รัน bakcup สำหรับ join to master หลัก
  kubeadm join keepalived-vir:80 --token <BOOTSTRAP_TOKEN> \
	--discovery-token-ca-cert-hash sha256:<CA_CERT_HASH> \
	--control-plane --certificate-key <CERTIFICATE_KEY>

Please note that the certificate-key gives access to cluster sensitive data, keep it secret!
As a safeguard, uploaded-certs will be deleted in two hours; If necessary, you can use
"kubeadm init phase upload-certs --upload-certs" to reload certs afterward.

Then you can join any number of worker nodes by running the following on each as root:
# # รัน worker สำหรับ join to master หลัก
kubeadm join keepalived-vir:80 --token <BOOTSTRAP_TOKEN> \
	--discovery-token-ca-cert-hash sha256:<CA_CERT_HASH> 

# # containerd
crictl --runtime-endpoint unix:///var/run/containerd/containerd.sock ps -a
crictl --runtime-endpoint unix:///var/run/containerd/containerd.sock logs ae10288b41c38








## check all service available should be running and existing haproxy-kubemaster1 and keepalived-kubemaster1
kubectl get pod -A

### example
kube-flannel   kube-flannel-ds-9fx4w                 1/1     Running   0          8s
kube-system    coredns-7db6d8ff4d-tqpm8              1/1     Running   0          2m19s
kube-system    coredns-7db6d8ff4d-xmgdp              1/1     Running   0          2m19s
kube-system    etcd-kubemaster1                      1/1     Running   16         2m30s
kube-system    haproxy-kubemaster1                   1/1     Running   3          2m30s
kube-system    keepalived-kubemaster1                1/1     Running   11         2m30s
kube-system    kube-apiserver-kubemaster1            1/1     Running   16         2m30s
kube-system    kube-controller-manager-kubemaster1   1/1     Running   16         2m30s
kube-system    kube-proxy-9gwd5                      1/1     Running   0          2m19s
kube-system    kube-scheduler-kubemaster1            1/1     Running   16         2m30s

## copy command after kubeadm init for multi controlplane and worker


kubeadm join controlplane:6443 --token <BOOTSTRAP_TOKEN> \
	--discovery-token-ca-cert-hash sha256:<CA_CERT_HASH> \
	--control-plane --certificate-key <CERTIFICATE_KEY>


kubeadm join controlplane:6443 --token <BOOTSTRAP_TOKEN> \
	--discovery-token-ca-cert-hash sha256:<CA_CERT_HASH>




## reset cluster

kubeadm reset
iptables --list
iptables --flush
ip address del 192.168.40.35/24 dev ens192